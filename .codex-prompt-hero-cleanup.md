Mobile homepage hero cleanup for innflow.ai (innflow-web). Screenshot attached: .codex-inbox-hero-cleanup.png (Instagram in-app browser, red circle = cluttered hero).

Do these UI changes only — no unrelated refactors:

1. KEEP the "Continue with Google" button (leave it as a primary CTA in the hero / flow). Do not remove Google auth CTA.
2. REMOVE the "See demo" button from the hero.
3. Change the footer CTA (the email/member signup card at the bottom of the hero — "A little more, just for members" / Sign up now) — simplify that CTA control so it fits mobile better (keep signup working). REMOVE the text below that CTA (the privacy / marketing disclaimer under Sign up now).
4. Make the "Get Innflow updates and member offers by email" copy smaller on mobile only.

Success criteria:
- Mobile first fold: no "See demo"; Google CTA remains; member email subtitle smaller on mobile; footer/email CTA text-under-button gone.
- Desktop should still look sane (don't break larger breakpoints).
- Prefer minimal diffs in the homepage/hero components.
