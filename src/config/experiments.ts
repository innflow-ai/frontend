// The site-wide layout experiment is retired. Keep its infrastructure dormant;
// future copy or asset experiments need their own keys and must share one shell.
// Environment modes, saved assignments, and QA URLs cannot reactivate this test.
export const MARKETING_LAYOUT_EXPERIMENT_ENABLED = false;
